# Base URL API backend.
# Opsi 1 (disarankan): relatif "/api" — frontend & API disajikan satu server (php artisan serve di port 8000).
# Opsi dev: "http://127.0.0.1:8000/api" jika frontend dijalankan terpisah (npm run dev + Vite proxy).
VITE_API_BASE_URL=/api
